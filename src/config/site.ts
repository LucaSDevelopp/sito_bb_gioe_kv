import { Gem } from 'lucide-react'

export const siteConfig = {
  name: "BB Gioie",
  description: "Creazioni uniche per momenti indimenticabili",
  logo: Gem, // Puoi sostituire questo con un'immagine importata o un componente SVG personalizzato
  accentColor: "grey", // Colore lucente per hover e accenti
}